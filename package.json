{
   "name": "web-geo-game-project",
   "private": true,
   "scripts": {
      "postinstall": "cd server && npm install && cd ../client && npm install",
      "dev": "concurrently \"npm run dev --prefix client\" \"cross-env DATA_URL=$DATA_URL npm run dev --prefix server\"",
      "build-server": "cd server && npm run build && ncp ./dist/ ../dist",
      "build-client": "cd client && npm run build && ncp ./dist/ ../dist/src/client/",
      "clean": "rimraf ./dist",
      "build": "npm run build-server && npm run build-client",
      "build:clean": "npm run clean && npm run build",
      "start": "cross-env NODE_ENV=production DATA_URL=$DATA_URL node ./dist/main.js"
   },
   "devDependencies": {
      "concurrently": "^7.0.0",
      "cross-env": "^7.0.3",
      "eslint": "^9.9.0",
      "ncp": "^2.0.0",
      "rimraf": "^6.0.1"
   },
   "dependencies": {
      "express": "^4.21.1",
      "react-confetti": "^6.4.0",
      "react-icons": "^5.6.0",
      "socket.io": "^4.8.1",
      "uuid": "^11.0.3"
   }
}
